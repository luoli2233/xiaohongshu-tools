{
  "name": "office-slack-game",
  "version": "0.2.6",
  "private": true,
  "type": "module",
  "description": "职场摸鱼：横屏离线 H5 演示版",
  "scripts": {
    "start": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check src/engine.js && node --check src/app.js && node --check src/scene.js && node --check src/storage.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
