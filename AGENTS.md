# 本工程开发约定

- 产品历史基线为 docs/reference/ 下的开发演示文档 v0.2；当前规则以 src/config.js、src/engine.js 和 CHANGELOG.md 为准。视觉基线为 docs/reference/ART_DIRECTION.md。不要覆盖原始设计文件。
- 这是 H5 游戏工程，不是内容网站。保留零运行依赖与规则/表现分离，除非后续需求明确需要引擎迁移。
- 所有经济数值只能由 engine.js 推进；渲染和按钮不能直接发经验或奖励。
- 改规则需同时更新配置版本、差异记录及对应行为测试。旧版本存档不能静默升级。
- 时间单位毫秒，100ms 固定步长，不能用画面帧数计奖励。
- 调试入口只能建立 demo 局；正式记录写入始终检查 demo 标记。
- 不修改或删除损坏的原始存档。恢复失败必须有提示，不补默认字段掩盖损坏。
- 使用可辨识像素动作，警告同时有文字；动作按钮不小于44px。保证移动端和键盘均可操作。
- 提交前执行 npm test、npm run check、npm run build。UI 改动另做实际浏览器检查，记录实际测试范围。
- 禁止将本地 H5 验收写成平台接入完成、真机兼容完成或已上线。
- 工程任务和缺陷统一维护 docs/03-工程管理.md；版本事实维护 CHANGELOG.md。
